export default function HomePage() {
  return (
    <section className="hero-card">
      <p className="eyebrow">SaaS clinic platform</p>
      <h1>Clinicall monorepo scaffold is ready.</h1>
      <p className="lead">
        This is the Next.js app entry point. The earlier UI was replaced to remove runtime dependency issues while the app workspace is being finalized.
      </p>
      <p className="lead">
        The full application will live here once the web dependencies are installed and the app is bootstrapped with the correct Next.js runtime.
      </p>
    </section>
  );
}
